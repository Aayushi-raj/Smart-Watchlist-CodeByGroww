DATABASE_URL="postgresql://postgres:password@localhost:5432/groww_watchlist?schema=public"

# Optional — if omitted, the insight engine uses a deterministic structured fallback
GEMINI_API_KEY=""

NODE_ENV=development
PORT=5000
